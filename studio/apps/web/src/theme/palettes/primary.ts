import { darkSemantic, lightSemantic } from "@/theme/palettes/semantic";

const lightPrimary: { main: string } = {
  main: lightSemantic.accent.primary,
};

const darkPrimary: { main: string } = {
  main: darkSemantic.accent.primary,
};

export { darkPrimary, lightPrimary };
